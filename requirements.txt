# =============================================================================
# PodMind — Python Dependencies
# AI-Driven Real-Time Pod Resource Discovery & Dependency Mapping
# =============================================================================

# -----------------------------------------------------------------------------
# Kubernetes & Infrastructure
# -----------------------------------------------------------------------------
kubernetes==28.1.0                  # Official K8s Python client
prometheus-api-client==0.5.5        # PromQL query executor with DataFrame output
redis[hiredis]==5.0.3               # Redis client with C-accelerated parser

# -----------------------------------------------------------------------------
# Web Framework & Server
# -----------------------------------------------------------------------------
fastapi==0.110.0                    # Async web framework with OpenAPI docs
uvicorn[standard]==0.29.0           # ASGI server with WebSocket support
websockets==12.0                    # WebSocket protocol library
starlette==0.36.3                   # ASGI toolkit (FastAPI dependency)

# -----------------------------------------------------------------------------
# Task Scheduling
# -----------------------------------------------------------------------------
apscheduler==3.10.4                 # Background job scheduler (5s/30s intervals)

# -----------------------------------------------------------------------------
# AI Agent Framework
# -----------------------------------------------------------------------------
langgraph==0.1.19                   # DAG-based agent orchestration
langchain>=0.2.0,<0.3.0             # Allows pip to grab 0.2.17 (the true end of the 0.2 line)
langchain-core>=0.2.22,<0.3.0       # Keeps LangGraph happy with its minimum requirement

# -----------------------------------------------------------------------------
# LLM Providers
# -----------------------------------------------------------------------------
anthropic==0.26.0                   # Anthropic Claude API (Tier 1)
openai==1.30.0                      # OpenAI GPT-4o API (Tier 2)
ollama==0.2.0                       # Ollama local LLM client (Tier 3)

# -----------------------------------------------------------------------------
# Machine Learning & Statistical Analysis
# -----------------------------------------------------------------------------
scikit-learn>=1.5.0                 # Python 3.13 native support
statsmodels>=0.14.4                  # Updated for modern Python runtimes
networkx==3.3                       # Graph data structures for dependency mapping
prophet>=1.1.6                      # Upgraded internal compiler parameters

# -----------------------------------------------------------------------------
# Data Processing
# -----------------------------------------------------------------------------
numpy>=1.26.0,<2.0.0
pandas>=2.2.0
scipy>=1.13.0

# -----------------------------------------------------------------------------
# Validation & Templating
# -----------------------------------------------------------------------------
pydantic>=2.9.0                     # Resolves 3.13-specific deprecation warnings
pydantic-settings>=2.5.0
jinja2==3.1.4                       # Prompt template rendering

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
httpx==0.27.0                       # Async HTTP client for kubelet API
python-dotenv==1.0.1                # .env file loading
loguru==0.7.2                       # Structured logging with rotation
tenacity==8.3.0                     # Retry logic for API calls
orjson==3.10.3                      # Fast JSON serialization

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest==8.2.0                       # Test framework
pytest-asyncio==0.23.7              # Async test support
pytest-cov==5.0.0                   # Coverage reporting
